# Classic Models GraphQL API - Environment Variables
#
# Note: Username and password are hardcoded in auth.graphql
#       TLS certificate is automatically loaded from certs/ca.pem by Makefile

# StepZen CLI Login (optional - only if using login.sh script)
# STEPZEN_DOMAIN=<your-stepzen-domain>
# STEPZEN_INSTANCE_ID=<your-instance-id>
# STEPZEN_API_KEY=<your-api-key>
# STEPZEN_INTROSPECTION=<your-introspection-url>
