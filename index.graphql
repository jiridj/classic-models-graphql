# -----------------------------------
# Unified entry point for GraphQL API
# -----------------------------------

schema @sdl(files: ["auth.graphql", "customers.graphql"]) {
  query: Query
  mutation: Mutation
}